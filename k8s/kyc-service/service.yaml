apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: sentinelpay
  labels:
    app.kubernetes.io/name: kyc-service
    app.kubernetes.io/part-of: sentinelpay
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kyc-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
