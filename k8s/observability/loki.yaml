# ---------------------------------------------------------------------------
# DEPRECATED — single-binary Loki (replaced by Helm-managed SimpleScalable)
#
# This file is kept for reference only. It is NOT included in kustomization.yaml.
# Loki is now installed via Helm; see loki-scalable-values.yaml.
#
# To install:
#   ./scripts/bootstrap.sh
#
# To upgrade:
#   helm upgrade loki grafana/loki \
#     --namespace sentinelpay \
#     --values k8s/observability/loki-scalable-values.yaml \
#     --reuse-values
# ---------------------------------------------------------------------------
